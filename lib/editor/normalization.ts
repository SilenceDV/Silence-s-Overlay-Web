import { defaultAnimation, defaultImage, defaultProject, defaultSlide, defaultText } from "./defaults";
import type { Layer, Project, Slide } from "@/types/editor";
const num=(v:unknown,f:number)=>typeof v==="number"&&Number.isFinite(v)?v:f;
const str=(v:unknown,f:string)=>typeof v==="string"?v:f;
const record=(v:unknown):Record<string,unknown>=>v&&typeof v==="object"?v as Record<string,unknown>:{};
export function normalizeLayer(input:unknown):Layer { const l=record(input); const a=record(l.animation); const animation={...defaultAnimation(),type:str(a.type,str(l.textAnimation??l.imageAnimation,"none")) as Layer["animation"]["type"],speed:num(a.speed,num(l.textAnimationSpeed??l.imageAnimationSpeed,1)),intensity:num(a.intensity,1),delay:num(a.delay,num(l.textLetterDelay,0))}; const common={id:str(l.id,crypto.randomUUID()),name:str(l.name,"Layer"),x:num(l.x,50),y:num(l.y,50),w:Math.max(1,num(l.w,28)),h:Math.max(1,num(l.h,28)),opacity:num(l.opacity,100),locked:Boolean(l.locked),animation}; if(l.type==="image"){const d=defaultImage(str(l.imageUrl??l.image,""),str(l.fileName,"Image")); return {...d,...common,type:"image",imageUrl:str(l.imageUrl??l.image,""),fileName:str(l.fileName,""),fit:l.fit==="cover"?"cover":"contain",cropX:num(l.cropX,0),cropY:num(l.cropY,0),cropZoom:num(l.cropZoom,100),imageWidth:num(l.imageWidth??l.imgW,common.w),imageHeight:num(l.imageHeight??l.imgH,common.h),outline:num(l.outline,0),outlineColor:str(l.outlineColor,"#ffffff"),glow:num(l.glow,0),glowColor:str(l.glowColor,"#00aaff")};} const d=defaultText(); return {...d,...common,type:"text",text:str(l.text,"New Text"),fontSize:num(l.fontSize,72),effect:l.effect==="gradient"||l.effect==="customGradient"?"gradient":"solid",color:str(l.color,"#57fff4"),gradient1:str(l.gradient1,"#00aaff"),gradient2:str(l.gradient2,"#ff7a00"),gradientAngle:num(l.gradientAngle,90),stroke:num(l.stroke,3)}; }
export function normalizeSlide(input:unknown):Slide {const s=record(input);const layers=Array.isArray(s.layers)?s.layers.map(normalizeLayer):[];const d=defaultSlide();return{...d,id:str(s.id,d.id),name:str(s.name??s.giftName,"New Gift"),entranceAnimation:str(s.entranceAnimation??s.animation,"none") as Slide["entranceAnimation"],duration:num(s.duration,0),layers:layers.length?layers:[defaultText()]};}
export function normalizeProject(input:unknown):Project {const p=record(input);const d=defaultProject();const slides=Array.isArray(p.slides)?p.slides.map(normalizeSlide):d.slides;return{...d,id:str(p.id,d.id),name:str(p.name,"Untitled Overlay"),slides:slides.length?slides:d.slides,settings:{...d.settings,...record(p.settings)},schemaVersion:2,updatedAt:str(p.updatedAt,new Date().toISOString())} as Project;}
