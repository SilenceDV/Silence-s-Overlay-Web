import "server-only";import type { SubscriptionRecord } from "@/types/billing";import { calculateEntitlements } from "@/lib/billing/entitlements";export const overlayIsActive=(enabled:boolean,subscription:SubscriptionRecord|null,now=new Date())=>{const entitlements=calculateEntitlements(subscription,now);return enabled&&entitlements.canPublish&&(subscription===null||entitlements.plan==="pro");};
