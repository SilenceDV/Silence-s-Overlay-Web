import "server-only";import type { SubscriptionRecord } from "@/types/billing";import { calculateEntitlements } from "@/lib/billing/entitlements";export const overlayIsActive=(enabled:boolean,subscription:SubscriptionRecord|null,now=new Date())=>enabled&&calculateEntitlements(subscription,now).canPublish&&(subscription===null||calculateEntitlements(subscription,now).plan==="pro");
