"use client"; import { useEffect } from "react"; import { isEditableTarget,nudgeAmount } from "@/lib/editor/keyboard"; import type { EditorApi } from "./useEditorState";
export function useKeyboardShortcuts(api:EditorApi,selected:string|null){useEffect(()=>{const fn=(e:KeyboardEvent)=>{const typing=isEditableTarget(e.target);if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==="z"){e.preventDefault();api.undo();return}if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==="y"){e.preventDefault();api.redo();return}if(!selected||typing)return;if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==="d"){e.preventDefault();api.duplicateLayer(selected);return}const n=nudgeAmount(e.shiftKey),delta:Record<string,[number,number]>={ArrowLeft:[-n,0],ArrowRight:[n,0],ArrowUp:[0,-n],ArrowDown:[0,n]};if(delta[e.key]){e.preventDefault();api.nudge(selected,...delta[e.key])}if(e.key==="Delete"||e.key==="Backspace"){e.preventDefault();api.deleteLayer(selected)}};window.addEventListener("keydown",fn);return()=>window.removeEventListener("keydown",fn)},[api,selected])}

