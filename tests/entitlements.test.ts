import { describe,expect,it } from "vitest";import { calculateEntitlements,enforceProjectEntitlements } from "@/lib/billing/entitlements";import type { SubscriptionRecord } from "@/types/billing";
const sub=(status:SubscriptionRecord["status"],end:string):SubscriptionRecord=>({userId:"u",stripeCustomerId:"c",stripeSubscriptionId:"s",status,currentPeriodEnd:end,cancelAtPeriodEnd:status==="canceled"});
describe("server entitlements",()=>{const now=new Date("2026-01-01T00:00:00Z");it("limits free projects to one slide",()=>{const e=calculateEntitlements(null,now);expect(e.maxSlides).toBe(1);expect(()=>enforceProjectEntitlements(2,e)).toThrow();});it("keeps canceled access through the paid period",()=>expect(calculateEntitlements(sub("canceled","2026-01-02T00:00:00Z"),now).plan).toBe("pro"));it("expires access after period end",()=>expect(calculateEntitlements(sub("canceled","2025-12-31T00:00:00Z"),now).plan).toBe("free"));});
