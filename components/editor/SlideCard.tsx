import type { Slide } from "@/types/editor";export function SlideCard({slide,index,active,onSelect,onChange,onDuplicate,onDelete}:{slide:Slide;index:number;active:boolean;onSelect:()=>void;onChange:(name:string)=>void;onDuplicate:()=>void;onDelete:()=>void}){return <article className={active?"slide-card active":"slide-card"} draggable onClick={onSelect}><span className="slide-number">{index+1}</span><input aria-label="Slide name" value={slide.name} onClick={e=>e.stopPropagation()} onChange={e=>onChange(e.target.value)}/><div className="row"><button onClick={e=>{e.stopPropagation();onDuplicate()}}>Duplicate</button><button className="danger" onClick={e=>{e.stopPropagation();onDelete()}}>Delete</button></div></article>}
