import type { EditorApi } from "@/hooks/useEditorState";import type { Layer } from "@/types/editor";
export function EditorToolbar({api,layer,onAddImage}:{api:EditorApi;layer:Layer|null;onAddImage:()=>void}){return <header className="editor-toolbar"><div><button onClick={api.addText}>T+</button><button onClick={onAddImage}>Image+</button></div><div><button disabled={!layer} onClick={()=>layer&&api.moveLayer(layer.id,1)}>Forward</button><button disabled={!layer} onClick={()=>layer&&api.moveLayer(layer.id,-1)}>Backward</button><button disabled={!layer} onClick={()=>layer&&api.duplicateLayer(layer.id)}>Duplicate</button><button disabled={!layer} onClick={()=>layer&&api.updateLayer(layer.id,{locked:!layer.locked})}>{layer?.locked?"Unlock":"Lock"}</button><button className="danger" disabled={!layer} onClick={()=>layer&&api.deleteLayer(layer.id)}>Delete</button></div><div><button onClick={api.undo}>Undo</button><button onClick={api.redo}>Redo</button></div></header>}
