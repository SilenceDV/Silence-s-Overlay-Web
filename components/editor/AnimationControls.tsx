import { IMAGE_ANIMATIONS,TEXT_ANIMATIONS } from "@/lib/animations/animationTypes"; import type { Layer,LayerAnimationSettings } from "@/types/editor";
export function AnimationControls({layer,onChange}:{layer:Layer;onChange:(a:LayerAnimationSettings)=>void}){const options=layer.type==="text"?TEXT_ANIMATIONS:IMAGE_ANIMATIONS;const a=layer.animation;return <fieldset><legend>Continuous animation</legend><label>Type<select value={a.type} onChange={e=>onChange({...a,type:e.target.value as LayerAnimationSettings["type"]})}>{options.map(x=><option key={x} value={x}>{x}</option>)}</select></label><label>Speed <output>{a.speed}s</output><input type="range" min=".2" max="8" step=".1" value={a.speed} onChange={e=>onChange({...a,speed:+e.target.value})}/></label><label>Intensity <output>{a.intensity}</output><input type="range" min=".1" max="3" step=".1" value={a.intensity} onChange={e=>onChange({...a,intensity:+e.target.value})}/></label><label>Delay <output>{a.delay}s</output><input type="range" min="0" max="3" step=".05" value={a.delay} onChange={e=>onChange({...a,delay:+e.target.value})}/></label></fieldset>}
