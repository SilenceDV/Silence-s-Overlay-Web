import type { EditorApi } from "@/hooks/useEditorState";import type { Project } from "@/types/editor";import { SlideCard } from "./SlideCard";
export function SlideList({project,current,api}:{project:Project;current:string;api:EditorApi}){return <section><h2>Slides</h2>{project.slides.map((s,i)=><SlideCard key={s.id} slide={s} index={i} active={s.id===current} onSelect={()=>api.selectSlide(s.id)} onChange={name=>api.updateSlide(s.id,{name})} onDuplicate={()=>api.duplicateSlide(s.id)} onDelete={()=>api.deleteSlide(s.id)}/>)}<button className="wide-button" onClick={api.addSlide}>+ Add slide</button></section>}
