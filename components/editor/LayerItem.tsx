import type { Layer } from "@/types/editor"; export function LayerItem({layer,selected,onSelect,onMove}:{layer:Layer;selected:boolean;onSelect:()=>void;onMove:(d:number)=>void}){return <li className={selected?"layer-item selected":"layer-item"}><button onClick={onSelect}>{layer.locked?"🔒 ":""}{layer.type==="text"?"T":"▧"} {layer.name}</button><span><button aria-label="Move layer down" onClick={()=>onMove(-1)}>↓</button><button aria-label="Move layer up" onClick={()=>onMove(1)}>↑</button></span></li>}
