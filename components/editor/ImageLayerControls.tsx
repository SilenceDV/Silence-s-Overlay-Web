import type { ImageLayer } from "@/types/editor";import { ColorPicker } from "./ColorPicker";import { AnimationControls } from "./AnimationControls";
export function ImageLayerControls({layer,onChange,onReplace}:{layer:ImageLayer;onChange:(p:Partial<ImageLayer>)=>void;onReplace:()=>void}){return <div className="controls-grid"><div className="row wide"><button onClick={onReplace}>Replace image</button><button onClick={()=>onChange({cropX:0,cropY:0,cropZoom:100})}>Reset crop</button></div><label>Fit<select value={layer.fit} onChange={e=>onChange({fit:e.target.value as ImageLayer["fit"]})}><option value="contain">Contain</option><option value="cover">Crop</option></select></label><label>Crop zoom <output>{layer.cropZoom}%</output><input type="range" min="20" max="300" value={layer.cropZoom} onChange={e=>onChange({cropZoom:+e.target.value})}/></label><label>Outline <input type="range" min="0" max="24" value={layer.outline} onChange={e=>onChange({outline:+e.target.value})}/></label><ColorPicker label="Outline color" value={layer.outlineColor} onChange={outlineColor=>onChange({outlineColor})}/><label>Glow <input type="range" min="0" max="60" value={layer.glow} onChange={e=>onChange({glow:+e.target.value})}/></label><ColorPicker label="Glow color" value={layer.glowColor} onChange={glowColor=>onChange({glowColor})}/><AnimationControls layer={layer} onChange={animation=>onChange({animation})}/></div>}
