"use client";export function BillingActions({hasCustomer}:{hasCustomer:boolean}){const go=async(path:string)=>{const r=await fetch(path,{method:"POST"}),d=await r.json();if(r.ok&&d.url)location.href=d.url;else alert(d.message||"Billing request failed")};return <div className="actions"><button onClick={()=>go("/api/billing/checkout")}>Upgrade or resubscribe</button><button disabled={!hasCustomer} onClick={()=>go("/api/billing/portal")}>Manage billing</button></div>}
