import {NextResponse} from "next/server";import {requireUser} from "@/lib/auth/server";import {createSupabaseAdminClient} from "@/lib/supabase/server";import {projectSchema} from "@/lib/validation/projectSchemas";import {enforceProjectEntitlements,getEntitlements} from "@/lib/billing/entitlements";import {apiError} from "@/lib/http";type C={params:Promise<{id:string}>};
export async function GET(_:Request,c:C){try{const user=await requireUser(),{id}=await c.params;const {data,error}=await createSupabaseAdminClient().from("projects").select("id,name,data,version,updated_at").eq("id",id).eq("owner_id",user.id).single();if(error)throw Object.assign(new Error("Project not found"),{code:"NOT_FOUND"});return NextResponse.json(data)}catch(e){return apiError(e,404)}}
export async function PUT(req:Request,c:C){try{const user=await requireUser(),{id}=await c.params,body=await req.json(),project=projectSchema.parse(body.project);if(project.id!==id)throw new Error("Project ID mismatch");enforceProjectEntitlements(project.slides.length,await getEntitlements(user.id));const db=createSupabaseAdminClient();const {data,error}=await db.from("projects").update({name:project.name,data:project,version:Number(body.version)+1,updated_at:new Date().toISOString()}).eq("id",id).eq("owner_id",user.id).eq("version",body.version).select("version,updated_at").maybeSingle();if(error)throw error;if(!data)return NextResponse.json({code:"STALE_VERSION",message:"A newer version of this project is already saved."},{status:409});return NextResponse.json(data)}catch(e){return apiError(e)}}
export async function PATCH(req:Request,c:C){try{const user=await requireUser(),{id}=await c.params,body=await req.json(),db=createSupabaseAdminClient();if(body.action==="duplicate"){const {data:source}=await db.from("projects").select("data").eq("id",id).eq("owner_id",user.id).single();if(!source)throw new Error("Project not found");const project=projectSchema.parse(structuredClone(source.data));project.id=crypto.randomUUID();project.name=`${project.name} copy`;project.updatedAt=new Date().toISOString();enforceProjectEntitlements(project.slides.length,await getEntitlements(user.id));const {data,error}=await db.from("projects").insert({id:project.id,owner_id:user.id,name:project.name,data:project,version:1}).select("id,name,version,updated_at").single();if(error)throw error;return NextResponse.json(data,{status:201})}const name=String(body.name||"").trim().slice(0,200);if(!name)throw new Error("Name is required");const {data,error}=await db.from("projects").update({name}).eq("id",id).eq("owner_id",user.id).select("id,name,version,updated_at").single();if(error)throw error;return NextResponse.json(data)}catch(e){return apiError(e)}}
export async function DELETE(_:Request,c:C){try{const user=await requireUser(),{id}=await c.params;const {error}=await createSupabaseAdminClient().from("projects").delete().eq("id",id).eq("owner_id",user.id);if(error)throw error;return new NextResponse(null,{status:204})}catch(e){return apiError(e)}}
