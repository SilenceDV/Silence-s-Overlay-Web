import {NextResponse} from "next/server";import {requireUser} from "@/lib/auth/server";import {getEntitlements} from "@/lib/billing/entitlements";import {apiError} from "@/lib/http";export async function GET(){try{return NextResponse.json(await getEntitlements((await requireUser()).id),{headers:{"Cache-Control":"no-store"}})}catch(e){return apiError(e,401)}}
