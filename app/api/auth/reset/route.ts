import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase/server";export async function POST(req:Request){const f=await req.formData();const {error}=await (await createSupabaseServerClient()).auth.updateUser({password:String(f.get("password")||"")});return NextResponse.redirect(new URL(error?`/reset-password?error=${encodeURIComponent(error.message)}`:"/reset-password?success=1",req.url),303)}
