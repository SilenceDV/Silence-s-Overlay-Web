import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase/server";export async function POST(req:Request){const f=await req.formData();await (await createSupabaseServerClient()).auth.resetPasswordForEmail(String(f.get("email")||""),{redirectTo:`${new URL(req.url).origin}/api/auth/callback?next=/reset-password`});return NextResponse.redirect(new URL("/login?message=If that account exists, a reset link was sent.",req.url),303)}
