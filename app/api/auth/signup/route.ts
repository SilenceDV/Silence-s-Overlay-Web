import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase/server";
export async function POST(req:Request){const form=await req.formData(),email=String(form.get("email")||""),password=String(form.get("password")||"");const db=await createSupabaseServerClient();const origin=new URL(req.url).origin;const {error}=await db.auth.signUp({email,password,options:{emailRedirectTo:`${origin}/api/auth/callback?next=/dashboard`}});if(error)return NextResponse.redirect(new URL(`/signup?error=${encodeURIComponent(error.message)}`,req.url),303);return NextResponse.redirect(new URL("/login?message=Check your email to verify your account.",req.url),303)}
