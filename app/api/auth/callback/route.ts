import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase/server";import {safeNext} from "@/lib/http";export async function GET(req:Request){const u=new URL(req.url),code=u.searchParams.get("code");if(code){const {error}=await (await createSupabaseServerClient()).auth.exchangeCodeForSession(code);if(!error)return NextResponse.redirect(new URL(safeNext(u.searchParams.get("next")),u.origin))}return NextResponse.redirect(new URL("/login?error=The sign-in link is invalid or expired.",u.origin))}
