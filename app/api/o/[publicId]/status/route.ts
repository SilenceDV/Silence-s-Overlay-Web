import {NextResponse} from "next/server";import {fetchOverlayState} from "@/lib/overlays/fetchOverlay";export async function GET(_:Request,{params}:{params:Promise<{publicId:string}>}){const {publicId}=await params,{active}=await fetchOverlayState(publicId);return NextResponse.json({active},{headers:{"Cache-Control":"no-store, max-age=0"}})}
