import type { Metadata } from "next";import "@/styles/globals.css";import "@/styles/editor.css";import "@/styles/controls.css";import "@/styles/animations.css";import "@/styles/overlay.css";export const metadata:Metadata={title:"Silence's Overlay Maker",description:"Create and publish stream overlays"};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
