import Link from "next/link";export default async function Signup({searchParams}:{searchParams:Promise<Record<string,string>>}){const q=await searchParams;return <main className="auth-page"><form className="card auth-card" action="/api/auth/signup" method="post"><h1>Create your account</h1>{q.error&&<p className="error">{q.error}</p>}<label>Email<input required type="email" name="email" autoComplete="email"/></label><label>Password<input required minLength={8} type="password" name="password" autoComplete="new-password"/></label><button>Start creating</button><p>Already have an account? <Link href="/login">Log in</Link></p></form></main>}
