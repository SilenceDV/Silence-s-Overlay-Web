import {fetchOverlayState} from "@/lib/overlays/fetchOverlay";import {OverlayClient} from "./OverlayClient";export const dynamic="force-dynamic";export const revalidate=0;export default async function OverlayPage({params}:{params:Promise<{publicId:string}>}){const {publicId}=await params,{overlay}=await fetchOverlayState(publicId);return <main className="overlay-page">{overlay?<OverlayClient project={overlay.snapshot} publicId={publicId}/>:null}</main>}
