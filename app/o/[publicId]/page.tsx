import { notFound } from "next/navigation";import { fetchOverlay } from "@/lib/overlays/fetchOverlay";import { OverlayClient } from "./OverlayClient";export const dynamic="force-dynamic";export default async function OverlayPage({params}:{params:Promise<{publicId:string}>}){const {publicId}=await params;const overlay=await fetchOverlay(publicId);if(!overlay)notFound();return <OverlayClient project={overlay.snapshot} publicId={publicId}/>}
