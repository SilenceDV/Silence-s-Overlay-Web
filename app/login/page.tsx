import Link from "next/link";export default async function Login({searchParams}:{searchParams:Promise<Record<string,string>>}){const q=await searchParams;return <main className="auth-page"><form className="card auth-card" action="/api/auth/login" method="post"><h1>Welcome back</h1>{q.error&&<p className="error">{q.error}</p>}{q.message&&<p className="success">{q.message}</p>}<input type="hidden" name="next" value={q.next||"/dashboard"}/><label>Email<input required type="email" name="email" autoComplete="email"/></label><label>Password<input required type="password" name="password" autoComplete="current-password"/></label><button>Log in</button><p><Link href="/forgot-password">Forgot password?</Link></p><p>New here? <Link href="/signup">Create an account</Link></p></form></main>}
