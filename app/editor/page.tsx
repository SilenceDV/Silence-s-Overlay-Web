import {redirect} from "next/navigation";import {OverlayEditor} from "@/components/editor/OverlayEditor";import {requireUser} from "@/lib/auth/server";import {createSupabaseAdminClient} from "@/lib/supabase/server";import {getEntitlements} from "@/lib/billing/entitlements";import {projectSchema} from "@/lib/validation/projectSchemas";
export default async function EditorPage({searchParams}:{searchParams:Promise<{id?:string}>}){const user=await requireUser(),{id}=await searchParams;if(!id)redirect("/dashboard");const [{data},e]=await Promise.all([createSupabaseAdminClient().from("projects").select("data,version").eq("id",id).eq("owner_id",user.id).maybeSingle(),getEntitlements(user.id)]);if(!data)redirect("/dashboard");return <OverlayEditor initialProject={projectSchema.parse(data.data)} projectId={id} version={data.version} proAccess={e.proAccess}/>}
